# ──────────────────────────────────────────────────────────────────────────────
# requirements.txt – Meeting Recorder & Summarizer
#
# Compatible with Python 3.10+.
#
# ⚠️  PyTorch notes
#   • For CPU-only:  pip install torch torchaudio --index-url
#     https://download.pytorch.org/whl/cpu
#   • For CUDA 11.8:  pip install torch torchaudio --index-url
#     https://download.pytorch.org/whl/cu118
# ──────────────────────────────────────────────────────────────────────────────

# --- core ASR ---
# whisperx is not on PyPI; install from source:
#   pip install git+https://github.com/m-bain/whisperx.git

# faster-whisper is pulled in by whisperx, pinned for stability
faster-whisper>=1.0.0

# --- speaker diarization ---
pyannote.audio>=3.2.0

# --- audio I/O ---
# ffmpeg-python is a thin Python wrapper; the system ffmpeg binary is also
# needed (sudo apt install ffmpeg)
ffmpeg-python>=0.2.0

# --- ML / tensor ---
# Torch & torchaudio are listed for reference but should be installed
# separately with the correct CUDA wheel for your environment (see note above).
# torch>=2.0.0
# torchaudio>=2.0.0

# --- utilities ---
huggingface-hub>=0.20.0

# --- GUI ---
# tkinter is part of Python's standard library (sudo apt install python3-tk).
# The recorder_app.py GUI also requires system packages:
#   sudo apt install ffmpeg pulseaudio

# --- summarization ---
# Uses Ollama runtime (https://ollama.com/download); no extra Python package
# is needed. Default model: hf.co/unsloth/Qwen3.5-9B-GGUF:Q4_K_M
